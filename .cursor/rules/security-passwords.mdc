---
description: Passwords with bcrypt; never plain text; unique salt
globs: src/**/*.ts
alwaysApply: false
---

# Security (passwords)

- **Passwords:** Never store or log in plain text. Store with **bcrypt**, unique random salt (stored with hash). Verify by hashing the attempt with the stored salt and comparing. Do not skip hashing for "dev only" or use weak/default salts.
